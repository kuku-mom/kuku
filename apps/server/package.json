{
  "name": "@kuku/server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "moon run server:dev",
    "start": "./bin/server",
    "deps:up": "docker compose -f test/docker-compose.test.yml up -d",
    "deps:down": "docker compose -f test/docker-compose.test.yml down"
  },
  "dependencies": {
    "@kuku/contract": "workspace:*"
  }
}
